# Streamlit UI

Run the dark support chat UI from this folder:

```bash
cd /Users/shubham/Desktop/Help_assistant/frontend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run streamlit_app.py
```

The app opens at `http://localhost:8501`.
